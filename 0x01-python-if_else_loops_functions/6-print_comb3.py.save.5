#!/usr/bin/python3
for x in range(0, 9):
    for y in range(0, 10):
        if x != 8 or y != 9:
            print("{}{},".format(x, y), end=" ")
        else:
            print("{}{}".format(x, y))
    x = x + 1
    y = a + 1
